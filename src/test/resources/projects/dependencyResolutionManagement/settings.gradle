pluginManagement {
	plugins {
		id 'com.crystaelix.loom-repositories'
	}
}

plugins {
	id 'com.crystaelix.loom-repositories'
}

dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
}

include 'basic'
include 'projmap'